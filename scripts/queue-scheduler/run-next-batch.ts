import { createRetainedBatchEnvelope, runNextBatch, QUEUE_SCHEDULER_FLAGS } from "../../src/lib/queue-scheduler";

void runNextBatch().then((result) => { console.log(JSON.stringify(createRetainedBatchEnvelope(result))); if (result.run.status === "partial") process.exitCode = 2; else if (result.run.status === "blocked_preflight") process.exitCode = 3; else if (result.run.status === "failed") process.exitCode = 4; }).catch((error: unknown) => { const value = error instanceof Error ? error.message : String(error); console.error(JSON.stringify({ schemaVersion: "daily69-retained-batch-result-v1", event: "queue_batch_failed", status: "failed", safeError: /^[A-Z0-9_:-]+$/u.test(value) ? value : "QUEUE_BATCH_FAILED", ...QUEUE_SCHEDULER_FLAGS })); process.exitCode = 4; });
